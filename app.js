console.log('Welcome!');

let dog = 'Redd';

console.log('My dog name is ${dog}');
console.log('${dog} is very cute.');

const label = document.getElementById('Label');
label.style.color = 'brown';

const button = document.getElementById{'button'};
button.addEventListenerbutton.addEventListener('click', ()=>{})
// 1. store the element with id email-inputs
const inputs = document.getElementById('Inputs');
// 2. add class 'hide' to the element
// with an id of email-inputs
// remove the class "hide" from the
inputs.classList.toggle('hide')
//3. store the element with id thank-you
// grab DOM elements
const Thankyou = document.getElementById('Thankyou');

Thankyou.classList.toggle('hide')
4. // set event listeners
// get user input
// use user input to update state 
// update DOM to reflect the new state